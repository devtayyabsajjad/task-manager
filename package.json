{
  "devDependencies": {
    "turbo": "^1.13.3"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "cd apps/backend && npm install && npm run vercel-build && cd ../frontend && npm install && npm run build",
    "vercel-build": "cd apps/backend && npm install && npm run vercel-build && cd ../frontend && npm install && npm run build",
    "start": "node apps/backend/dist/main",
    "deploy-check": "node scripts/pre-deploy-check.js"
  },
  "workspaces": [
    "apps/*"
  ],
  "dependencies": {
    "chart.js": "^4.5.0",
    "react-chartjs-2": "^5.3.0",
    "superagent": "^9.0.0"
  }
}
